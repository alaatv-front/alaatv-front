const getters = {
  pageBuilderEditable: (state) => {
    return state.pageBuilderEditable
  },
  setList: (state) => {
    return state.setList
  },
  setTopicList: (state) => {
    return state.pageBuilderEditable
  },
  selectedTopic: (state) => {
    return state.pageBuilderEditable
  }
}

export default getters
